% Generated by roxygen2: do not edit by hand
\name{sce_plot_pathway_rds_continuous}
\alias{sce_plot_pathway_rds_continuous}
\title{Batch-plot spatial pathway scores from RDS files}
\usage{
sce_plot_pathway_rds_continuous(
  object,
  score_dir = "gmt_pathway_scores",
  selected_rds = NULL,
  selected_pathways = NULL,
  selected_images = NULL,
  output_dir = "Spatial_pathway_continuous",
  pathway_match = c("exact", "contains", "regex"),
  image_scale = c("lowres", "hires"),
  smooth_k = 8,
  sigma_factor = 0.8,
  mask_radius_factor = 1.3,
  grid_n = 300,
  colors = "magma",
  show_he = TRUE,
  he_alpha = 0.40,
  surface_alpha = 0.82,
  crop = TRUE,
  crop_padding = 0.02,
  xlim = NULL,
  ylim = NULL,
  contour = FALSE,
  contour_bins = 8,
  show_spots = FALSE,
  transform = "none",
  cap_quantile = c(0.01, 0.99),
  save_pdf = TRUE,
  save_png = TRUE,
  width = 7,
  height = 6,
  dpi = 300,
  keep_plots = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{object}{A Seurat object. For virtual knockout analysis, also accepts a genes-by-cells count matrix or an RDS/QS file containing one of these objects.}
\item{score_dir}{Directory containing pathway score RDS files.}
\item{selected_rds}{RDS paths or filenames within score_dir; NULL selects all RDS files there.}
\item{selected_pathways}{Pathway names or patterns; NULL selects all numeric pathways.}
\item{selected_images}{Spatial image names; NULL selects all images.}
\item{output_dir}{Output directory; defaults and NULL behavior are shown in Usage. NULL disables exports for gene-expression statistics and selects the configuration directory for DEG.}
\item{pathway_match}{Match pathway names exactly, by case-insensitive literal substring, or by regular expression.}
\item{image_scale}{Spatial coordinate scale: lowres or hires.}
\item{smooth_k}{Number of nearest spots in Gaussian smoothing.}
\item{sigma_factor}{Gaussian bandwidth in multiples of median spot spacing.}
\item{mask_radius_factor}{Maximum distance from a measured spot in multiples of spot spacing.}
\item{grid_n}{Number of grid positions along x; y adapts to the aspect ratio.}
\item{colors}{A color vector or named palette: viridis, magma, inferno, plasma, cividis, turbo, blue_red, blue_yellow_red, white_red or navy_yellow_red.}
\item{show_he}{Overlay the stored H&E image when available.}
\item{he_alpha}{H&E image opacity.}
\item{surface_alpha}{Interpolated surface opacity.}
\item{crop}{Crop to the observed spot extent.}
\item{crop_padding}{Relative padding around the cropped extent.}
\item{xlim}{Optional x limits, overriding automatic cropping.}
\item{ylim}{Optional y limits, overriding automatic cropping.}
\item{contour}{Overlay contour lines.}
\item{contour_bins}{Number of contour levels.}
\item{show_spots}{Overlay original spot centers.}
\item{transform}{Value transformation: none, log1p, sqrt or zscore.}
\item{cap_quantile}{Two ordered clipping probabilities, or NULL to disable clipping.}
\item{save_pdf}{Save each spatial map as PDF.}
\item{save_png}{Save each spatial map as PNG.}
\item{width}{Saved plot width in inches.}
\item{height}{Saved plot height in inches.}
\item{dpi}{Resolution for raster output.}
\item{keep_plots}{Retain ggplot objects in the returned list.}
\item{verbose}{Print progress messages.}
}
\value{Invisibly, a list with plots (retained only when keep_plots is TRUE), summary, log and images.}
\description{Read pathway score tables, align barcodes and generate continuous spatial maps across selected pathways, RDS files and images.}
\details{Accepts RDS matrices, data frames, Seurat metadata, or lists containing a score table. Spot-by-pathway and pathway-by-spot orientations are recognized by barcode matches, and barcode columns are supported. Only numeric score columns with finite values are used. Each pathway needs at least ten valid spots. Temporary metadata is added to a local copy. PNG and PDF files are saved by default, with pathway_plot_summary.csv and pathway_plot_log.csv when results exist. Calls sce_plot_spatial_continuous directly; no manual source call is needed. Missing files and individual failed plots are reported and skipped; inspect the returned log.}

