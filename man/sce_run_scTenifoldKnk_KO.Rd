% Generated by roxygen2: do not edit by hand
\name{sce_run_scTenifoldKnk_KO}
\alias{sce_run_scTenifoldKnk_KO}
\title{Run virtual gene knockouts with scTenifoldKnk}
\usage{
sce_run_scTenifoldKnk_KO(
  object,
  gKO,
  output_dir = "scTenifoldKnk_KO",
  assay = "RNA",
  layer = "counts",
  subset_col = NULL,
  subset_values = NULL,
  cells = NULL,
  features = NULL,
  prefilter_genes = TRUE,
  min_cells_gene = 25,
  max_genes = 5000,
  max_cells = NULL,
  min_ko_positive_cells = 10,
  ignore_gene_case = TRUE,
  qc = FALSE,
  qc_mtThreshold = 0.10,
  qc_minLSize = 1000,
  qc_minCells = 25,
  nc_lambda = 0,
  nc_nNet = 10,
  nc_nCells = 500,
  nc_nComp = 3,
  nc_scaleScores = TRUE,
  nc_symmetric = FALSE,
  nc_q = 0.90,
  td_K = 3,
  td_maxIter = 1000,
  td_maxError = 1e-5,
  td_nDecimal = 3,
  ma_nDim = 2,
  fdr_cutoff = 0.05,
  top_n = 20,
  label_n = 10,
  save_network_plot = TRUE,
  network_q = 0.99,
  network_annotate = FALSE,
  network_nCategories = 20,
  run_enrichment = FALSE,
  enrichr_databases = NULL,
  seed = 1234,
  nCores = 2
)
}
\arguments{
\item{object}{A Seurat object. For virtual knockout analysis, also accepts a genes-by-cells count matrix or an RDS/QS file containing one of these objects.}
\item{gKO}{One or more knockout genes; each is analyzed separately.}
\item{output_dir}{Output directory; defaults and NULL behavior are shown in Usage. NULL disables exports for gene-expression statistics and selects the configuration directory for DEG.}
\item{assay}{Assay name. For spatial plotting, NULL uses the default assay.}
\item{layer}{Expression layer or slot. Virtual knockout requires raw counts. Spatial plotting can choose an available layer when NULL.}
\item{subset_col}{Optional metadata column used to select a cell population.}
\item{subset_values}{Values to retain in subset_col.}
\item{cells}{Optional cell names to retain.}
\item{features}{Optional gene names to retain, in addition to KO genes.}
\item{prefilter_genes}{Filter genes by minimum number of expressing cells, retaining KO genes.}
\item{min_cells_gene}{Minimum expressing cells for the manual gene filter.}
\item{max_genes}{Optional cap ranked by detection frequency; KO genes are forced to remain. NULL disables the cap.}
\item{max_cells}{Optional maximum cell count; larger inputs are randomly downsampled.}
\item{min_ko_positive_cells}{Warn when a KO gene is detected in fewer cells.}
\item{ignore_gene_case}{Allow an unambiguous case-insensitive match of KO names.}
\item{qc}{Apply the backend quality-control filters.}
\item{qc_mtThreshold}{Maximum mitochondrial count fraction for backend QC.}
\item{qc_minLSize}{Minimum cell library size for backend QC.}
\item{qc_minCells}{Minimum expressing-cell count parameter for backend QC; see version adaptation in Details.}
\item{nc_lambda}{Network regularization parameter passed to scTenifoldKnk.}
\item{nc_nNet}{Number of subsampled networks.}
\item{nc_nCells}{Cells per network, capped at the number available.}
\item{nc_nComp}{Principal components for network construction; must exceed two.}
\item{nc_scaleScores}{Scale principal-component regression scores.}
\item{nc_symmetric}{Construct symmetric networks.}
\item{nc_q}{Network weight quantile threshold.}
\item{td_K}{Rank of the tensor decomposition.}
\item{td_maxIter}{Maximum tensor-decomposition iterations.}
\item{td_maxError}{Tensor-decomposition convergence tolerance.}
\item{td_nDecimal}{Decimal precision for tensor decomposition.}
\item{ma_nDim}{Number of manifold-alignment dimensions.}
\item{fdr_cutoff}{Adjusted P-value threshold for perturbed genes.}
\item{top_n}{Number of top genes reported and plotted.}
\item{label_n}{Number of genes labeled in the perturbation plot.}
\item{save_network_plot}{Save the backend KO-centered network plot.}
\item{network_q}{Edge-weight quantile for the KO network plot.}
\item{network_annotate}{Query enrichment services to annotate network nodes.}
\item{network_nCategories}{Maximum annotation categories in the network plot.}
\item{run_enrichment}{Run optional online Enrichr enrichment of significant perturbed genes.}
\item{enrichr_databases}{Enrichr database names; NULL selects available GO BP, KEGG and Reactome libraries.}
\item{seed}{Random seed, with the caller's random-number state restored afterwards.}
\item{nCores}{Positive integer number of backend worker cores.}
}
\value{Invisibly, a named list of per-gene bundles containing result, diffRegulation, significant, top, plots, manifold_data, enrichment, parameters, input_summary and run_dir.}
\description{Select cells and raw-count genes, run one or more virtual knockouts, and export perturbation tables, figures, parameters and analysis bundles.}
\details{Uses raw nonnegative counts, not normalized expression. Each KO is run separately. The FC statistic measures perturbation and is not a signed gene-expression log fold change. Gene caps always retain requested KO genes and may therefore be exceeded by those forced genes. The original QC argument names remain supported: scTenifoldKnk 1.1 and later receive qc_maxMTratio, qc_minLibSize and qc_minPCT; the latter is qc_minCells divided by the number of selected input cells, capped at one. Filtering details remain those of the installed backend version. Random-number state is restored on return. Network annotation and optional enrichR analysis access the Enrichr service and submit gene identifiers when enabled. Default outputs include XLSX tables, PNG/PDF figures, raw results and analysis-bundle RDS files, parameters and summary TXT files, and session information.}
\examples{
\dontrun{
sce <- readRDS("annotated_seurat.rds")
ko <- sce_run_scTenifoldKnk_KO(
  object = sce,
  gKO = c("RAMP1", "GABBR1"),
  subset_col = "celltype",
  subset_values = "Macrophage",
  output_dir = "scTenifoldKnk_KO",
  nCores = 4
)
}
}
