% Generated by roxygen2: do not edit by hand
\name{sce_plot_spatial_continuous}
\alias{sce_plot_spatial_continuous}
\title{Plot a continuous spatial expression or metadata surface}
\usage{
sce_plot_spatial_continuous(
  object,
  feature,
  image = NULL,
  assay = NULL,
  layer = NULL,
  image_scale = c("lowres", "hires"),
  smooth_k = 12,
  sigma_factor = 1.0,
  grid_n = 300,
  mask_radius_factor = 1.5,
  transform = c("none", "log1p", "sqrt", "zscore"),
  cap_quantile = c(0.01, 0.99),
  colors = "magma",
  n_colors = 100,
  na_color = "transparent",
  show_he = TRUE,
  he_alpha = 0.55,
  surface_alpha = 0.80,
  crop = TRUE,
  crop_padding = 0.03,
  xlim = NULL,
  ylim = NULL,
  contour = FALSE,
  contour_bins = 8,
  contour_color = "white",
  contour_alpha = 0.35,
  contour_size = 0.3,
  show_spots = FALSE,
  spot_size = 0.15,
  spot_alpha = 0.35,
  spot_color = "black",
  reverse_y = TRUE,
  title = NULL,
  legend_title = NULL,
  legend_position = "right",
  save_path = NULL,
  width = 8,
  height = 7,
  dpi = 300,
  return_data = FALSE
)
}
\arguments{
\item{object}{A Seurat object. For virtual knockout analysis, also accepts a genes-by-cells count matrix or an RDS/QS file containing one of these objects.}
\item{feature}{One gene or numeric metadata column to visualize.}
\item{image}{Spatial image name; NULL selects the first image.}
\item{assay}{Assay name. For spatial plotting, NULL uses the default assay.}
\item{layer}{Expression layer or slot. Virtual knockout requires raw counts. Spatial plotting can choose an available layer when NULL.}
\item{image_scale}{Spatial coordinate scale: lowres or hires.}
\item{smooth_k}{Number of nearest spots in Gaussian smoothing.}
\item{sigma_factor}{Gaussian bandwidth in multiples of median spot spacing.}
\item{grid_n}{Number of grid positions along x; y adapts to the aspect ratio.}
\item{mask_radius_factor}{Maximum distance from a measured spot in multiples of spot spacing.}
\item{transform}{Value transformation: none, log1p, sqrt or zscore.}
\item{cap_quantile}{Two ordered clipping probabilities, or NULL to disable clipping.}
\item{colors}{A color vector or named palette: viridis, magma, inferno, plasma, cividis, turbo, blue_red, blue_yellow_red, white_red or navy_yellow_red.}
\item{n_colors}{Number of interpolated colors.}
\item{na_color}{Color for missing or masked surface values.}
\item{show_he}{Overlay the stored H&E image when available.}
\item{he_alpha}{H&E image opacity.}
\item{surface_alpha}{Interpolated surface opacity.}
\item{crop}{Crop to the observed spot extent.}
\item{crop_padding}{Relative padding around the cropped extent.}
\item{xlim}{Optional x limits, overriding automatic cropping.}
\item{ylim}{Optional y limits, overriding automatic cropping.}
\item{contour}{Overlay contour lines.}
\item{contour_bins}{Number of contour levels.}
\item{contour_color}{Contour line color.}
\item{contour_alpha}{Contour line opacity.}
\item{contour_size}{Contour line width.}
\item{show_spots}{Overlay original spot centers.}
\item{spot_size}{Spot point size.}
\item{spot_alpha}{Spot point opacity.}
\item{spot_color}{Spot point color.}
\item{reverse_y}{Reverse the y axis to match image coordinates.}
\item{title}{Optional plot title; defaults to the feature name.}
\item{legend_title}{Optional legend title; defaults to the feature name.}
\item{legend_position}{Legend position passed to ggplot2.}
\item{save_path}{Optional filename for saving the individual spatial plot.}
\item{width}{Saved plot width in inches.}
\item{height}{Saved plot height in inches.}
\item{dpi}{Resolution for raster output.}
\item{return_data}{Return the spot table, interpolated surface and parameters with the plot.}
}
\value{A ggplot, or when return_data is TRUE a list with plot, spot_data, surface_data and parameters.}
\description{Align a gene or numeric metadata feature with spatial spots, apply Gaussian nearest-neighbor smoothing, mask distant regions and optionally overlay an H&E image.}
\details{Metadata columns take precedence over gene names. At least ten matched, finite spots are required. Smoothing and masking are measured in multiples of median spot spacing. Transformations precede quantile clipping. The surface is a visualization of interpolated values, not additional measurements. The selected image scale must match the resolution of the image stored in the Seurat spatial object. Custom color vectors are accepted, including biomed_colors().}

